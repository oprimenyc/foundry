# FOUNDRY_SECRET_REMEDIATION_CURRENT_TRUTH

**GATE:** entering **GATE 1 — READ & RECONCILE** (per `PrimeOS/ECOSYSTEM_OS_AUDIT/01_CONSTITUTION.md` §0 taxonomy — GATE 0 orient confirmed below, GATE 1 evidence gathered in this document; conflicts recorded, not silently resolved).

## GATE 0 — Orient

- **Foundry path:** `C:\Users\jp718\foundry` (confirmed).
- **Branch:** `mission/m3-vault-intelligence`.
- **HEAD @ start:** `90e09b7ae9ddff329eaa960dd307530a61347673`.
- **Working tree:** clean, nothing to commit. No dirty files to preserve.
- Branch is ahead of `origin/mission/m3-vault-intelligence` by 9 commits (not pushed by this mission).

## Foundry conventions read (mission-relevant)

- **Evidence/artifacts:** `lib/foundry/evidence-manifest.ts` (canonical JSON hashing, HMAC/RSA-PSS/external-KMS signers), `lib/foundry/artifacts.ts` (`retainArtifact` — content-addressed, redacted-before-hash, retention-classed local file store; `verifyArtifactIntegrity`, `listArtifacts`).
- **Approvals/gates:** `lib/foundry/human-gates.ts` (persisted `ApprovalGateRecord`, pending/approved/rejected/expired/deferred, TTL expiry, decide-once immutability) — scoped to `DeploymentRun`/`PlanStep`, not reusable as-is for a non-execution-engine feature. `lib/vault/policy.ts` (`classifyActionRisk`, kill switches, `evaluatePolicy`) and `lib/vault/execution-gate.ts`/`lib/vault/approvals.ts` are the vault-side analog.
- **Provider routing:** `lib/foundry/providers.ts` (registry-per-category, mock-vs-real adapter selection gated by env credential presence, `assertMockAllowed` fails closed in production). Not used directly — this mission's adapters are advisory-only stubs, never registered as executable `ProviderAdapter`s, and never gain a live-call escape hatch (unlike the email-qa Resend boundary, which does have one behind two explicit flags — deliberately not mirrored here per the mission's absolute "no real provider calls" constraint).
- **Execution envelope / verification:** `lib/foundry/execution.ts`, `envelope.ts`, `verification.ts` — deployment-run-scoped; not extended by this mission (secret remediation has no deployment run).
- **Secret redaction:** `lib/vault/redaction.ts` — three-layer redaction (key-based, taint-registry value-based, pattern-based: Bearer/Basic, URL-embedded creds, `sk_live_`/`ghp_`/`AKIA`/`xox` shapes). `retainArtifact` calls `redactString` before hashing/writing, so stored artifacts are redacted-then-hashed. This mission adds its own pre-storage raw-secret scan as defense-in-depth (Task 1/7 requirement: raw values rejected outright, not merely redacted).
- **Closest architectural analog — `lib/email-qa/`:** provider-neutral `types.ts` (zod contract) + pure `validate.ts` (verdict logic) + `evidence.ts` (ties into `retainArtifact`) + `adapters/` (transport boundary, dry-run default) + `fixtures/` (real-repo-derived, read-only loader) + `FOUNDRY_EMAIL_QA_CONTRACT.md`. This mission's module (`lib/secret-remediation/`) mirrors this shape exactly, since the same design rules apply (provider-neutral contract, adapters are the only vendor-aware layer, evidence via the existing artifact backend, no new central-store collection needed for findings/plans/evidence).
- **Operator surface convention:** `app/api/ops/route.ts` + `lib/foundry/ops.ts` — `resolvePrincipal` auth gate, GET returns an aggregate report, POST is an action-discriminated union (`z.union` of literal-tagged schemas). Mirrored for `app/api/secret-remediation/route.ts`.
- **Test/proof conventions:** `node:test` + `node:assert/strict`, `tests/*.test.ts` run via `npm test` (`node --import tsx --test`), and a matching `scripts/<feature>-proof.ts` (see `scripts/email-qa-proof.ts`) that writes a JSON evidence bundle to `proof/evidence/` and asserts real-provider-calls-made is always false.
- **dyln fixture integration (`428403f`, `90e09b7`):** `lib/email-qa/fixtures/dyln-loader.ts` reads dyln's real fixture files read-only from `C:\REPLIT PROJECTS\dyln\dyln`, never imports dyln source, independently re-validates fixture shape, and stamps the dyln repo's own HEAD/branch into the resulting evidence bundle. This mission's PantiCandy/vITALCore fixtures follow the same spirit but are authored directly in `lib/secret-remediation/fixtures/*.ts` (not loaded from the product repos at runtime) because the source material is a set of already-completed containment findings (markdown reports), not machine-readable fixture files in those repos.

## Read-only fixture source docs (no secret values read)

- `C:\Users\jp718\PrimeOS\reports\ecosystem-deployment-repo-audit.md` — independent ecosystem-wide deployment audit; corroborates both PantiCandy findings (PAT in `.git/config`, historical `.env` with production Neon `DATABASE_URL`) and the vITALCore finding (`.env` currently tracked in git, `NEXTAUTH_SECRET`/`GOOGLE_CLIENT_SECRET`/`DATABASE_URL` committed).
- `C:\REPLIT PROJECTS\Panticandy\Panticandy\PANTICANDY_SECRET_CONTAINMENT_{CURRENT_TRUTH,ACTIONS,PROOF}.md` — GitHub PAT embedded in local `.git/config` `remote.origin.url`; **already locally contained** (URL rewritten to `https://github.com/oprimenyc/partigoddess.git`); rotation/revocation explicitly NOT done (owner action).
- `C:\REPLIT PROJECTS\Panticandy\Panticandy\PANTICANDY_ENV_HISTORY_{CURRENT_TRUTH,ACTIONS,PROOF}.md` — `.env` (carrying the production Neon `DATABASE_URL`) was committed at `04c356cf3cf4c467c704220f51a71b38c0415884` (2026-03-25) and removed at `3c1fed415c5e0d988de96ad34949d2d7a737b005` (2026-03-28); never scrubbed from history; repo HEAD at assessment `7494cc1209bfa341b0db2a25c9b40eb30fde61e2`. Currently untracked + gitignored going forward, but history exposure remains; rotate-vs-history-rewrite is an explicit unresolved owner decision.
- `C:\Users\jp718\OneDrive\Desktop\_SORTED\Active_Projects\vitalcore\VITALCORE_ENV_CONTAINMENT_{CURRENT_TRUTH,ACTIONS,PROOF}.md` — `.env` was **actively tracked** on branch `repositioning-batch-3` (repo HEAD at assessment `f880dda76923229b8cb24ea2b4ea5fb9869a53dd`), containing `DATABASE_URL`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `NEXTAUTH_SECRET`, `NEXTAUTH_URL`, plus two `NEXT_PUBLIC_*` (non-secret) vars. `git rm --cached .env` was already performed (index fixed); the same content is confirmed already pushed to `origin/repositioning-batch-3` across 4 historical commits, so history exposure is live on the remote, not just local. Rotation and history-rewrite decision are both outstanding owner actions.

**Conflict recorded, not silently resolved:** the mission brief's abbreviated hashes (`7494cc1`, `a05bb43`, `e664ccf`, `0747671`) do not all match verbatim commits found in the source docs above — `7494cc1` matches the PantiCandy **env-history** doc's assessment-time HEAD (not the PAT case, as the brief's phrasing implied), and `a05bb43`/`e664ccf`/`0747671` were not found in any read-only source doc searched (`ecosystem-deployment-repo-audit.md` and siblings, PantiCandy's three containment docs, vITALCore's three containment docs). The fixtures in this mission use the **verified** hashes quoted directly from each source doc above (`04c356c`/`3c1fed4` for the PantiCandy `.env` add/remove pair, `7494cc1...`/`f880dda...` as assessment-time repo HEADs) rather than the brief's abbreviated references, since those are what the source-of-truth documents actually contain. The underlying facts (PAT exposure, historical `.env` exposure, vITALCore tracked `.env` with the three named secret categories) are independently confirmed by both the product repos' own containment docs and PrimeOS's separate ecosystem audit — only the specific short-hash citations in the mission brief don't line up.

## Scope confirmation

- Write boundary: Foundry only (`C:\Users\jp718\foundry`). No files were modified in PrimeOS, PantiCandy, or vITALCore — all three were read-only inputs.
- No real GitHub/Supabase/Neon/Google/Railway API calls will be made. No credentials rotated. No secret values read, stored, or printed anywhere in this mission's code, tests, fixtures, or docs — only categories, fingerprint-shaped hashes (never computed from a real secret), and commit/file metadata.

GATE: proceeding to **GATE 2 — SPECIFY** (contract + plan-engine + gate + adapter design) immediately below, then **GATE 3 — EXECUTE**.
