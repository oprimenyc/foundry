# Foundry Completion Ledger

Evidence-backed record of verified milestones. Newest last. Companion docs:
`FOUNDARY_RUNTIME_STATUS.md` (2026-07-10 audit), `FOUNDRY_CONTINUATION.md` (resume instructions).

| Milestone | Initial state | Changes | Evidence | Commit | Remaining risk |
| --------- | ------------- | ------- | -------- | ------ | -------------- |
| Provider registry + Vercel HTTP adapter | Hardcoded github/vercel switch; orphaned Vercel client; runtime untracked in git | Generic ProviderRegistry, fail-closed lookup, local-git adapter, real Vercel adapter behind VERCEL_API_TOKEN, GET /api/providers, runtime committed | 13/13 tests, build, mock deploy/rollback proof | `9e53e42` | Vercel deploy/verify actions still fail closed (not implemented live) |
| UI run lifecycle + dead route removal | UI used stateless /api/plan preview; useDeploymentStream subscribed to a bus key no publisher wrote; healthz claimed "supabase" from an env var controlling nothing | UI drives project→plan→run→SSE→rollback; stream repointed to durable run events; SSE flushes headers immediately; dead /projects/[id]/logs route removed; test+proof npm scripts | typecheck, 13/13, build, mock e2e, live smoke 6/6 | `97ff095` | /api/plan preview route retained (still functional, exercised by smoke) |
| Production-safe SQLite persistence | Only file persistence; productionSafe could never be true; production execution permanently blocked | SqlitePersistence (node:sqlite, WAL, synchronous=FULL, transactional writes); FOUNDRY_PERSISTENCE mode selection fails closed on unknown values; live read/write health probe; atomic temp+rename file writes | typecheck, 17/17, build, proofs in file+sqlite modes, live healthz `production_safe_persistence:true` | `556e18b` | Single-node scope; node:sqlite is experimental in Node 22; whole-store JSON document (no per-row constraints) |
| Step timeout + retry policy | plan timeoutMs/retryLimit validated but never enforced; retryCount never incremented | executeWithTimeout race; retry loop honoring retryLimit; ProviderError with retryable/category; truthful failureCategory incl. timeout | typecheck, 20/20 (retry-to-success, timeout-exhaustion, non-retryable), build, proof | `b5cf11d` | Timed-out adapter promise is abandoned, not aborted (no AbortSignal in adapter contract) |
| API auth, fail-closed production | Every route unauthenticated; runs could trigger real provider spend anonymously | FOUNDRY_API_TOKEN bearer + httpOnly session cookie (for EventSource); production 503 fail-closed without token; dev open; session login route; UI token prompt; healthz reports auth mode | typecheck, 23/23, build, live smoke 7/7 with auth enforced | `111fcb5` | Single shared token (no per-user identity/tenancy); dev default is open |
| Live Vercel deployment actions | trigger_deployment/verify_deployment failed closed as unimplemented; no deployment cancel compensation | createDeployment/getDeployment/cancelDeployment/waitForDeployment in the HTTP client; adapter executes + verifies deployments, records deploymentId/URL references, cancels on compensation; injectable HTTP client | typecheck, 27/27 (stubbed-transport request-shape, polling, ERROR, cancel tests), build, mock proof | `1916c64` | No live-API proof yet (needs VERCEL_API_TOKEN — human blocker) |
| Truthful docs + dead-code removal | README/.env.example described removed routes and a nonexistent Supabase persistence path; lib/db/supabase.ts, api-keys.ts, migrations had zero call sites | Orphans removed; README/.env.example rewritten to match runtime (auth, persistence modes, verification commands, ops notes) | typecheck, 27/27, build | `691bd4d` | — |
| Boot-time crash recovery | resumeIncompleteRuns existed with zero app call sites — crashes stranded runs forever | instrumentation.ts register() resumes incomplete runs at server boot; next.config.mjs instrumentationHook; crash-recovery proof script | typecheck, 23/23, build, live proof: seeded queued run in sqlite completed by fresh server process (`{recovered:true}`) | `c20a83b` | Recovery races with very fast concurrent requests are unexercised |
| Real GitHub repository adapter | mock only | GitHubHttpAdapter with read-back verification, unsafe-name rejection, classified errors, delete-compensation; production mock fail-closed; offline+live proof script | 32/32, offline github proof | `f796167` | live proof needs GITHUB_TOKEN |
| Organization tenancy | hardcoded org_local, one shared token | Principal model (FOUNDRY_PRINCIPALS / FOUNDRY_API_TOKEN+FOUNDRY_ORG_ID), org-scoped service layer + routes incl. SSE/cancel/rollback, 404 anti-enumeration, requestedBy audit actor, dev bootstrap principal | 35/35, cross-tenant tests | `44702a9` | single-token-per-principal (no rotation) |
| Durable-event consolidation | redundant subscriber-less log bus | bus removed; durable events sole source; delivery cannot affect run results; healthz reports events:durable-store | 35/35, mock proof | `8c593db` | — |
| Independent verification (E.V.E.-ready) | runs certified themselves | append-only VerificationRecords via injectable external check; verify API routes org-scoped; verdict separate from run status; history immutable | 37/37, extended e2e proof | `b087c16` | live verification needs reachable URLs |
| Domain expansion + launch profiles | repository+deployment only; closed action enum | open action namespace (registry-gated); dns/email/payments/telephony/storage registries; real cloudflare/resend/stripe/signalwire adapters (offline-proven); capability metadata API; provider matrix + DYLN/VERIDIAN/CoS/Rising Promise profiles | 41/41, all proofs | `d05fe35` | live proofs credential-blocked; storage prod adapter demand-deferred |
| Final gate proofs | — | DYLN profile proof (8 steps/6 providers, verified, rolled back); FOUNDRY_ALLOW_MOCKS=explicit-test-mode loud override for prod-build test harnesses; recovery+smoke re-proven on final build; smoke asserts org-scoped SSE 404 | 41/41, recovery {recovered:true}, smoke 7/7, profile proof | `26d9434` | — |
