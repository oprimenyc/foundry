{
  "project": "panticandy",
  "providerType": "database",
  "actionType": "rotate_credential",
  "targetEnvironment": "production",
  "targetDescription": "Historical Supabase/Postgres connection credential referenced in PantiCandy's prior git history (predates the PAT containment pass), not yet rotated",
  "forcePushRequired": false,
  "rollbackPlan": [
    "Keep the previous connection string documented for emergency rollback only until the new one is verified stable, then discard it."
  ],
  "verificationPlan": [
    "The application connects successfully using the newly rotated connection string in every environment.",
    "The old connection string no longer authenticates against the database."
  ],
  "preApprovedGateReasons": [],
  "knownPrerequisiteGaps": [],
  "sourceReference": "PANTICANDY_SECRET_CONTAINMENT_ACTIONS.md item 4: \"Separate follow-up mission (not addressed here): historical .env exposure containment for old Supabase/Postgres credentials that may exist in prior git history.\"",
  "notes": "This action plan is the flagged follow-up from the PAT containment pass. Whether a git-history rewrite is also warranted for this exposure is a distinct, separate decision — see the git_history_rewrite_advisory action type, not bundled into this rotation plan."
}
